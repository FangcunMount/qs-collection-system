# npm 配置文件
# 使用 legacy peer deps 解决 Taro 3.x 的依赖冲突问题
legacy-peer-deps=true

# 使用国内镜像加速（可选，根据需要启用）
# registry=https://registry.npmmirror.com

# 严格的 SSL 检查
# strict-ssl=true

# 保存时使用精确版本（推荐）
save-exact=false

# 自动安装 peer dependencies
# auto-install-peers=true
