# Qlume 视觉资产清单

## 资产预算

- Banner 单文件不超过 200 KiB。
- 其他新增主包图片默认不超过 100 KiB。
- 分类小图标不超过 32 KiB。
- Banner 只承载背景和插图，标题、说明、状态、按钮必须使用 DOM。
- 新资源优先替换旧资源；`npm run check:ui-assets` 阻止未登记超限资产进入仓库。

## 2026 首页视觉资产

| 资产 | 领域/用途 | 版本 | 文件大小 | 建议显示 | 裁切 | 替代文本 |
|---|---|---:|---:|---|---|---|
| `src/assets/hero/qlume-home-v2.webp` | 总首页成长路径 Banner | v2 | 23,888 B | 1200×600 比例 | 居中 cover，保留左侧安静区 | 通往远方的温和成长路径 |
| `src/pages/catalog-medical/assets/hero/medical-catalog-v2.webp` | 医学目录专业评估 Banner | v2 | 22,298 B | 1200×600 比例 | 居中 cover，保留左侧文字区 | 纸质评估表与书写工具 |
| `src/pages/catalog-ability/assets/hero/ability-catalog-v2.webp` | 行为能力家庭成长 Banner | v2 | 27,282 B | 1200×600 比例 | 居中 cover，人物不可被裁掉 | 家庭通过观察与积木支持儿童成长 |

三张资产均为 2026-07-14 定向生成候选的人工选定版本，经裁切、缩放并以 WebP 压缩入库；源候选不进入小程序包。2026-09-05 从当前 PNG 原图重新编码 WebP（cwebp 质量 85，1200×600，未裁切），复核后接入上述路径；PNG 源图保存在 `docs/design/assets/catalog-heroes/`。

## 生成方向记录

- 总首页：编辑式柔和插画，使用光、路径、生长意象；左侧留白；禁止文字、按钮、UI 面板和霓虹效果。
- 医学目录：克制的临床编辑插画，使用纸张、笔和细微勾选意象；禁止 3D 剪贴板、营销徽章和屏幕界面。
- 行为能力：家庭观察、积木、植物和成长路径；禁止发光大脑、机器人、神经网络和伪科技 HUD。

## 人格资产策略

人格探索现有吉祥物可以继续使用，但必须保证透明边缘干净、光源一致、显示尺寸明确；同屏只保留一个主要角色焦点。人格品牌 PNG 不进入通用 `Icon` 注册表，由领域组件直接管理。

## AI 补充解读设计候选（未接入运行时）

| 资产 | 领域/用途 | 版本 | 文件大小 | 建议显示 | 裁切 | 替代文本 | 包归属 |
|---|---|---|---:|---|---|---|---|
| `docs/design/assets/ai-explanation/report-lens-v1-source.png` | 医学 AI 解读插图源候选 | v1 | 1,012,412 B | 源图 1254×1254 | 不裁切 | 报告页与放大镜 | 仅设计目录 |
| `docs/design/assets/ai-explanation/report-lens-v1.webp` | AI 入口、准备、等待与成品标题预览 | v1 | 671,688 B | 88 / 176 / 52px 方形 | contain | 装饰使用时为空 | 仅设计目录 |

2026-09-05 使用内置 image_gen 定向生成；母题为纸质报告与放大镜，医学蓝配少量蓝紫，不包含文字、分数或医疗批准标记。完整提示词与来源见 [资产记录](assets/ai-explanation/README.md)。WebP 为无损编码预览，超过主包 100 KiB 预算；实施阶段须导出预算内版本并复核后才能接入运行时。

## 登记字段

新增或替换资产必须登记：用途、领域、源文件/生成方向、版本、实际字节数、显示尺寸、替代文本、允许裁切方式、主包或分包归属。没有登记的生成初稿不得直接上线。


## AI 补充解读运行时资产

| 资产 | 用途 | 版本 | 实际大小 | 显示尺寸 | 替代文本/裁切 | 包归属 |
|---|---|---|---:|---|---|---|
| `src/pages/assessment/ai-explanation/assets/report-lens-v1.webp` | 医学报告 AI 入口、准备/等待、成品标识 | v1 | 13,730 B | 88 / 176 / 52px | 装饰为空；contain 不裁切 | assessment 分包资源路径 |

源图为本清单登记的内置 image_gen v1 候选；运行时导出 352×352 WebP，保留透明背景，符合新增图片 100 KiB 预算。最终打包归属以构建产物为准。

## 医学目录线条图标（2026-09-08）

按用户确认的医学首页、列表设计实施：医学首页移除大幅 Banner，改用四枚分类线条图标；列表移除缩略图。总首页、人格与行为能力页不变。历史图片保留，医学首页不再引用上述 Banner。

通用操作继续通过 `shared/ui/Icon` 使用已安装的 Taroify 0.9.2 图标，无新依赖。参考：https://taroify.github.io/taroify.com/components/icon/ 。四枚医学分类图标为本项目原创 SVG，非 AI 位图：32×32 viewBox、1.7 圆头线条，颜色对应 `@color-medical-700`（更换主题时需同步 SVG）。

| 文件（相对 `src/pages/catalog-medical/assets/icon/`） | 用途 | 字节数 | 显示与裁切 | 包归属 | 替代信息 |
|---|---|---:|---|---|---|
| `sleep.svg` | 睡眠分类 v1 | 278 | 64rpx 方形，aspectFit，不裁切 | 医学分包 | 相邻分类文字提供名称 |
| `mood.svg` | 情绪分类 v1 | 283 | 64rpx 方形，aspectFit，不裁切 | 医学分包 | 相邻分类文字提供名称 |
| `pressure.svg` | 压力分类 v1 | 280 | 64rpx 方形，aspectFit，不裁切 | 医学分包 | 相邻分类文字提供名称 |
| `attention.svg` | 执行功能分类 v1 | 389 | 64rpx 方形，aspectFit，不裁切 | 医学分包 | 相邻分类文字提供名称 |

列表筛选使用接口提供的适用年龄与填写者字段；展开筛选时加载当前类别的全部分页，缺失字段不推断。设计图的“查看详情”调整为当前真实填写流程对应的“开始评估”；没有新增详情路由或模拟量表数据。底部导航继续复用现有组件。

## 受试者首页人物（2026-09-08）

总首页改为受试者身份、静态人物、关注入口、评估记录的结构，旧 Qlume Hero 已退出首页引用。四张人物由内置 image_gen 依据用户确认的首页设计生成透明 PNG，再通过 cwebp 转为 480×720 WebP（质量 82）。不含文字、底座或场景，底座、气泡、卡片均由真实页面组件承载。

| 文件（相对 `src/assets/home/subjects/`） | 用途 | 字节数 | 显示与裁切 | 包归属 |
|---|---|---:|---|---|
| `adult-male.webp` | 成年男性静态形象 v1 | 32928 | aspectFit 完整人物，不裁切；高度由首页 Token 控制 | 主包 |
| `adult-female.webp` | 成年女性静态形象 v1 | 28844 | aspectFit 完整人物，不裁切；高度由首页 Token 控制 | 主包 |
| `child-male.webp` | 男童静态形象 v1 | 37068 | aspectFit 完整人物，不裁切；高度由首页 Token 控制 | 主包 |
| `child-female.webp` | 女童静态形象 v1 | 37496 | aspectFit 完整人物，不裁切；高度由首页 Token 控制 | 主包 |

人物为非真人、通用身份提示；相邻受试者姓名、年龄提供文字信息。生日不足 18 周岁使用儿童形象，满 18 周岁使用成人形象；无有效生日或性别时使用中性图标，图片失败可点击重试。年龄分组仅用于首页表现，不代表具体量表的适用范围。生成提示词、源图路径和实现边界见 [首页实施记录](decisions/subject-home-v1.md)。
